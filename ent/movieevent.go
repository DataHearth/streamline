// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/datahearth/streamline/ent/movie"
	"github.com/datahearth/streamline/ent/movieevent"
)

// MovieEvent is the model entity for the MovieEvent schema.
type MovieEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID uint32 `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Type holds the value of the "type" field.
	Type movieevent.Type `json:"type,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload map[string]interface{} `json:"payload,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MovieEventQuery when eager-loading is set.
	Edges        MovieEventEdges `json:"edges"`
	movie_events *uint32
	selectValues sql.SelectValues
}

// MovieEventEdges holds the relations/edges for other nodes in the graph.
type MovieEventEdges struct {
	// Movie holds the value of the movie edge.
	Movie *Movie `json:"movie,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MovieOrErr returns the Movie value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MovieEventEdges) MovieOrErr() (*Movie, error) {
	if e.Movie != nil {
		return e.Movie, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: movie.Label}
	}
	return nil, &NotLoadedError{edge: "movie"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MovieEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case movieevent.FieldPayload:
			values[i] = new([]byte)
		case movieevent.FieldID:
			values[i] = new(sql.NullInt64)
		case movieevent.FieldType:
			values[i] = new(sql.NullString)
		case movieevent.FieldCreateTime, movieevent.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case movieevent.ForeignKeys[0]: // movie_events
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MovieEvent fields.
func (_m *MovieEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case movieevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint32(value.Int64)
		case movieevent.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				_m.CreateTime = value.Time
			}
		case movieevent.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				_m.UpdateTime = value.Time
			}
		case movieevent.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				_m.Type = movieevent.Type(value.String)
			}
		case movieevent.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case movieevent.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field movie_events", value)
			} else if value.Valid {
				_m.movie_events = new(uint32)
				*_m.movie_events = uint32(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MovieEvent.
// This includes values selected through modifiers, order, etc.
func (_m *MovieEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryMovie queries the "movie" edge of the MovieEvent entity.
func (_m *MovieEvent) QueryMovie() *MovieQuery {
	return NewMovieEventClient(_m.config).QueryMovie(_m)
}

// Update returns a builder for updating this MovieEvent.
// Note that you need to call MovieEvent.Unwrap() before calling this method if this MovieEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MovieEvent) Update() *MovieEventUpdateOne {
	return NewMovieEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MovieEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MovieEvent) Unwrap() *MovieEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MovieEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MovieEvent) String() string {
	var builder strings.Builder
	builder.WriteString("MovieEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("create_time=")
	builder.WriteString(_m.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(_m.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", _m.Type))
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.Payload))
	builder.WriteByte(')')
	return builder.String()
}

// MovieEvents is a parsable slice of MovieEvent.
type MovieEvents []*MovieEvent
