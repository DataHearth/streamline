{{- if and .Values.enabled .Values.grafana.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: streamline-dashboard
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "observability.labels" . | nindent 4 }}
data:
  streamline.json: |
{{ .Files.Get "dashboards/streamline.json" | indent 4 }}
{{- end }}
